name: Shortlinks
description: A simple, easily manageable, shortlinks solution using Jekyll and GitHub Pages

collections:
  shortlinks:
    output: true

defaults:
  - scope:
      path: ""
      type: "shortlinks"
    values:
      layout: "redirect"